# 컨텍스트 노트 — 에이전트 구조 단순화

작성 2026-07-26. 결정 사항과 그 이유를 남긴다. 다음 세션이 근거를 다시 캐지 않도록.

---

## 왜 손대는가 — 확인된 사실 3가지

### 1. 최감수(review)는 존재하지 않는 문서를 검토하고 있었다

`useAgents.ts`의 `AGENT_IDS` 6개가 `Promise.allSettled`로 **동시에** 출발한다.
이 시점에서 `docgen`은 문서가 아니라 **체크포인트 질문**을 만든다(`useAgents.ts:389`,
`promptId = agentId === "docgen" ? "docgen_questions" : agentId`).
실제 문서는 변호사가 체크포인트에 답한 **뒤에** `useDocument.ts:196`에서 생성된다.

그런데 `buildReviewPrompt()`(`prompts.ts:2124`)는 이렇게 묻는다.

- "이 서면을 처음 받았을 때 판사가 느낄 인상"
- "인용된 법조문이 존재하며 정확한가"
- "빈칸(○○)이 최소화되어 있는가"

받는 컨텍스트에도 문서가 없다(`buildContextBlock(ctx)`만 전달). **아직 쓰이지 않은 문서를
100점 만점으로 채점하고 있었다.** 그 결과가 `DocumentPage.tsx:158`에서 `reviewResult`로
문서 생성 프롬프트에 다시 주입되고 있었다 — 즉 허수 평가가 문서 품질에 노이즈로 작용.

추가로 `Promise.allSettled`는 전원이 끝나야 다음으로 넘어가므로, **변호사가 다음에 할 일
(체크포인트 답변)이 이 무의미한 채점 뒤에 줄 서 있었다.**

### 2. 판서와 오사서는 둘 다 판례를 분석하고 있었다

`precedent`(한판서)는 법제처 API로, `rag_precedent`(오사서)는 RAG DB로. RAG 결과는 검색
자료일 뿐 별도 인격일 이유가 없다. 게다가 `useAgents.ts:215`의 RAG 대상 목록에
**`precedent`가 빠져 있어서** 한판서는 RAG를 못 받고 있었다.

→ 오사서를 없애고 `precedent`를 RAG 목록에 넣어 한판서가 법제처+RAG 양쪽을 받게 했다.
`AGENT_SEARCH_CONFIG.precedent`는 이미 잘 설정돼 있었다(5개 테이블). 설정은 손대지 않았다.

### 3. 법률 분석은 원래 쪼개면 안 되는 일이다

쟁점이 정해져야 어떤 판례가 중요한지 알 수 있고, 판례가 나와야 논증 구조가 잡히고,
관할이 정해져야 전략이 달라진다. 지금은 이 넷이 서로를 못 본 채 병렬로 나온 뒤
`buildAgentResultsBlock()`(`prompts.ts:283`)이라는 **수동 접착 코드**로 이어붙고 있다.
그 함수의 존재 자체가 "한 덩어리였어야 할 걸 쪼개놨다"는 증거다.

→ 이번 단계에서는 실행만 4개로 줄였고, 통합은 4~5단계로 미뤘다(스트리밍 선행 필요).

---

## 이번에 내린 결정과 이유

### 결정 1 — 타입·Firestore 스키마는 건드리지 않았다

`AgentId` 유니온(`types/agent.ts:6`)과 `LegalDocument.agentResults`(`types/document.ts:26-29`)에
`review`/`rag_precedent`가 그대로 남아 있다. **의도한 것이다.**
프로덕션 Firestore에 이미 이 필드를 가진 문서가 쌓여 있고, 과거 문서를 읽어 표시하는
경로(`UnifiedTimelineTab.tsx:537`, `AgentsPage.tsx:537`)가 살아 있어야 한다.
실행만 멈추면 비용·지연 이득은 100% 얻으면서 마이그레이션 위험은 0이다.

### 결정 2 — `AGENT_IDS`를 `AGENTS`에서 파생시켰다

기존에는 `config/constants.ts`의 `AGENTS`(UI용)와 `useAgents.ts`의 `AGENT_IDS`(실행용)가
따로 관리되고 있었다. `AgentsPage.tsx:359`가 `AGENTS`를 순회하므로, 둘이 어긋나면
**실행되지 않는 에이전트 카드가 영원히 로딩 상태로 남는다.**
→ `AGENT_IDS = AGENTS.map(a => a.id)`로 단일 진실원본화. 앞으로 에이전트를 늘리거나 줄일 때
`AGENTS` 한 곳만 고치면 된다.

### 결정 3 — `agentResults.rag_precedent` 슬롯은 살려뒀다 (중요)

이 슬롯은 에이전트 결과 저장소가 아니라 **체크포인트 첨부파일을 문서 생성으로 나르는
통로**로 쓰이고 있다.

- `CheckpointPage.tsx:354` — PDF/이미지 OCR 추출 텍스트를 이 슬롯에 이어붙임
- `DocumentPage.tsx:148` — 그 값을 `transcript`(대화록) 자리로 전달

즉 오사서의 판례 분석이 "대화록"인 척 문서 생성에 들어가고 있었다. 오사서를 끄면
이 슬롯에는 첨부파일 텍스트만 남으므로 **오히려 정상화된다.**

→ 다만 이름이 실제 용도와 다르다. `attachedFileContents` 같은 이름으로 바꾸고
`AgentContext.fileContents`(`prompts.ts:112`)로 보내는 게 맞다. 라우터 state 형태를
3개 페이지에 걸쳐 바꿔야 해서 이번 범위에서 제외했다. **후속 작업 목록에 있음.**

### 결정 4 — 마케팅 문구는 건드리지 않았다

`LandingPage.tsx`와 `pages/seo/AiAgentsPage.tsx`는 `data/landingContent.tsx`의 별도
`AGENTS` 상수를 쓴다(`config/constants.ts`와 무관). 따라서 코드는 안 깨진다.
다만 여전히 "6명의 전문 AI 에이전트"로 홍보 중이고, `PLANS`의 Pro 설명에도
"6개 에이전트"가 있다. **외부에 보이는 문구라 임의로 고치지 않았다 — 대표님 결정 사항.**

산출물이 여전히 판례·쟁점·관할·문서 관점을 담으므로 "여러 관점 분석"은 유지 가능하지만,
"6개 AI가 동시에 일합니다" 류의 표현은 사실과 달라진다.

---

## 비용 근거 (2026-07-26 공식 문서 기준)

| 모델 | 입력 $/MTok | 출력 $/MTok |
|---|---|---|
| Opus 5 | 5 | 25 |
| Sonnet 5 | 3 (도입가 2, **2026-08-31 종료**) | 15 (도입가 10) |
| Haiku 4.5 | 1 | 5 |

건당 비용 추정 (40분 상담, 환율 1,400원 가정)

| 구조 | 건당 |
|---|---|
| 현재 (Sonnet 전량, 도입가) | ~920원 |
| 현재 (Sonnet 전량, 9/1 이후) | ~1,390원 |
| 전량 Opus 5 | ~2,310원 |
| 목표 구조 (핵심 2곳만 Opus) | ~1,180원 |

Pro 요금제 ₩89,000/월 무제한 기준 손익분기는 전량 Opus 시 **월 38건**.
→ 3단계 계측이 끝나면 이 추정치를 실측으로 교체할 것.

## 아직 남은 함정

1. **`effort` 미지정** — Opus 5/Sonnet 5는 미지정 시 `high`가 기본이고 생각 기능이 켜진다.
   생각 토큰은 **출력으로 과금되고 `max_tokens` 안에 포함**된다. 현재 `MAX_TOKENS = 16384`가
   생각+본문을 함께 덮고 있어 `claude.ts`의 잘림 경고 경로가 실제로 발동하고 있을 것이다.
2. **6개 동시 호출로 프롬프트 캐시 무효화** — 캐시는 첫 응답이 시작된 뒤에야 읽을 수 있다.
   동시 출발이면 전원이 캐시 쓰기 요금(1.25배)만 낸다. 에이전트가 4개로 줄어 완화됐을 뿐
   원인은 그대로. 팬아웃 직전 `max_tokens: 0` 예열 요청이 정석.
3. **캐시 블록에 정작 큰 게 빠져 있다** — `SHARED_AGENT_PREFIX`(2,056자)만 캐시되고,
   정작 반복되는 대화록·첨부파일·RAG 결과는 캐시 안 되는 두 번째 블록에 들어간다.
4. **`stop_reason: "refusal"` 미처리** — `extractText()`가 빈 content를 만나 예외를 던진다.
   Opus 5는 거절을 정상 응답(HTTP 200)으로 돌려주므로 전환 전 반드시 처리.

---

# 2026-07-26 (오후) — 보안·결제·배포

## ⚠️ 오독 주의 — "Creating the secret"은 "값이 없었다"는 뜻이 아니다

**결론부터: 프로덕션 결제는 정상 작동하고 있었다.** 아래는 한 번 잘못 판단했던 기록이라
같은 실수를 반복하지 않도록 남긴다.

Cloudflare Pages는 **평문 환경변수(Environment variables)** 와 **Secret**을 따로 관리한다.

- `wrangler pages secret list` → **Secret만** 보여준다. 평문 환경변수는 안 나온다
- `wrangler pages secret put` → 같은 이름의 평문 환경변수가 있어도 **"Creating the secret"** 이라고 뜬다

`FIREBASE_CLIENT_EMAIL`·`FIREBASE_PRIVATE_KEY`가 `secret list`에 안 보이길래 "없다"고
판단했지만, 실제로는 **평문 환경변수로 프로덕션에 들어 있었다.** 프리뷰 환경에만 없어서
프리뷰가 500을 뱉은 것이고, 그 신호를 프로덕션까지 확대 해석한 것이 오류였다.

**환경변수 존재 여부는 `secret list`가 아니라 Cloudflare 대시보드에서 확인할 것.**
(Settings → Environment variables / Secrets 두 곳을 모두 봐야 한다)

### 실제로 한 일

`.dev.vars`의 키를 로컬에서 검증(Google 토큰 발급 HTTP 200)한 뒤 프로덕션에 **Secret으로**
등록했다. 값은 동일하므로 동작 변화는 없고, **개인키가 평문으로 저장돼 있던 것을 암호화
저장으로 승격**한 셈이라 보안상으로는 개선이다.
(전역 메모의 2026-04-23 "평문 → Secret 재등록" 건이 실제로는 반영돼 있지 않았던 것으로 보인다)

→ **남은 확인**: 대시보드에 같은 이름의 **평문 환경변수가 아직 남아 있는지** 볼 것.
남아 있다면 Secret과 중복이므로 평문 쪽을 지우는 게 깔끔하다.

프리뷰 환경은 wrangler 4.68.1이 `--environment` 플래그를 지원하지 않아 등록하지 못했다.
**프리뷰에서 서버 Firestore 기능을 테스트하려면 wrangler 업그레이드가 먼저다.**

## 🔴 signing_requests가 전 세계에 열려 있었다 (해결됨)

규칙에 `allow read, update: if true`가 있었다. 주석은 "토큰으로 조회"였지만 규칙에는
토큰 검증이 없었다 — 검증은 앱 코드의 `where("token","==",...)`에만 있었으므로
Firestore REST API를 직접 치면 무력화됐다. 수임계약서 전문 유출 + 제3자 변조가 가능했다.

문서 ID를 토큰으로 바꾸는 방법(마이그레이션 필요) 대신 **서버 검증**을 택했다.
`functions/api/signing/[token].ts`가 서비스 계정으로 조회/서명하고, 규칙에서는 `if true`를
제거했다. 서명자 IP는 클라이언트가 보낸 값이 아니라 `CF-Connecting-IP`를 쓴다.

## 배포에서 밟은 함정 두 개

**1. 브랜치에서 `npm run deploy`하면 프리뷰로 간다.**
`wrangler pages deploy`는 현재 git 브랜치로 프로덕션/프리뷰를 판단한다.
프로덕션에 올리려면 `main`에서 배포해야 한다.

**2. `scripts/post-deploy-check.sh`가 프로덕션 URL을 하드코딩하고 있다.**
`PROD_URL="https://law-caddy.pages.dev"` — 브랜치에서 배포하면 **방금 올린 코드가 아니라
구버전 프로덕션을 검사**하고 "모든 헬스체크 통과"를 띄운다. 초록 메시지를 믿으면 안 된다.
→ 개선 여지: wrangler 출력의 배포 URL을 받아서 검사하도록 바꾸면 좋다.

**3. 커스텀 도메인은 전파가 늦다.** 배포 직후 `law-caddy.com`은 구버전 Functions를
잠시 더 서빙했다(신규 공개 경로가 401). 1~2분 뒤 정상화됐다. 배포 검증 시 감안할 것.

## 2026-07-27 — 프로덕션 실측 A/B (브라우저 자동화)

로그인된 사용자 브라우저로 같은 사건을 구코드/신코드에서 각각 실행해 계측을 대조했다.

**기준선 (구코드 — SW가 서빙하던 이전 번들, 예열 없음):**
8회 호출 · 입력 55,266 · 출력 36,348 · 캐시쓰기 7,823 · 캐시읽기 5,637
→ 운영규칙 조각(1,879)만 캐시되고 사건 자료는 매번 제값. 진단대로였다.

**신코드 1차 (캐시 재설계 + 예열):**
예열 3.4초 · 팬아웃 4건 중 3건이 프리픽스(2,085)를 캐시읽기. **1건은 다시 씀 —
예열만 effort=low여서 캐시 버킷이 갈렸다.** speed처럼 effort도 캐시를 가르는
요청 파라미터다. 예열의 effort 지정을 제거해 본 호출과 맞췄다(배포됨).
+ 부수 발견: 키워드 추출이 판서·율무·혜안에서 같은 인자로 3번 나가고 있었다
(회당 ~470토큰, 구코드부터). Promise 메모로 1회로 합쳤다(배포됨).

**미완:** effort 정합 후 4/4 캐시읽기 최종 확인 1회. 아래 브라우저 문제로 중단.

## ⚠️ 검증 중 이 브라우저(개발 PC)의 law-caddy.com이 깨졌다

A/B를 위해 SW 강제 해제 + CacheStorage 전부 삭제를 반복한 여파로,
**law-caddy.com origin에서만** 엔트리 JS의 script-타입 로드가 계속 실패한다
(fetch는 200, 쿼리 붙이면 import 성공, pages.dev는 완전 정상, curl 정상).
하드리로드·SW 재설치·HTTP 캐시 강제 갱신으로도 안 풀렸다 — Chrome 내부의
origin 단위 잔재로 추정. **다른 사용자·다른 기기는 무관**(서버는 정상임을
pages.dev와 curl로 확인).
→ 복구: Chrome 완전 재시작, 안 되면 설정 → 개인정보 → 사이트 데이터에서
law-caddy.com만 삭제. 교훈: **SW 디버깅은 시크릿 창에서 할 것.**

## index.html의 크롤러 폴백은 별도 관리 대상이다

`#root` 안에 검색·AI 크롤러용 정적 폴백이 하드코딩돼 있다. landingContent만
고치면 이 블록이 낡은 채 남아 크롤러에게 모순된 카피가 나간다(실제로 6명·오사서
·36종이 그대로 있었다 — 2026-07-27 갱신). **랜딩 카피를 바꾸면 index.html도 함께.**

## 배포 순서 (외워둘 것)

앱 코드 → **그다음** 규칙. 순서를 뒤집으면 아직 구버전이 떠 있는 프로덕션에서
`if true`에 의존하던 서명이 즉시 깨진다.

## 2026-07-27 브라우저 먹통 사건의 진범 = 좀비 서비스워커 (종결)

전날 "law-caddy.com에서만 스크립트 로드 실패"의 원인이 확정됐다.
**활성 서비스워커가 구버전 precache HTML을 계속 서빙**하고 있었다
(`caches` 안에 workbox-precache 잔존, 그 HTML이 이미 삭제된 index-BuXcxau3.js를 참조 → 앱 영원히 미기동).
해제 절차: 페이지 컨텍스트에서 `getRegistrations()` 전부 unregister + `caches` 전부 delete
+ **쿼리 붙인 주소로 리로드**(`/record?fresh=...`, HTTP 캐시 우회) → 즉시 정상.
재발 방어로 빌드 산출물 파일명을 `assets/app-[hash]`로 변경(vite.config, SSR 빌드는 제외 — prerender가 entry-server.js를 찾음).

## 캐시 재설계 최종 실측 — 팬아웃 4/4 적중 (2회 재현)

운영 배포에서 두 번 실측, 두 번 모두 동일 패턴.
예열 1회만 캐시쓰기(2,415 / 2,130), 이후 **에이전트 4건 전부 캐시읽기, 추가 쓰기 0**.
effort 파라미터가 캐시 버킷을 가르는 문제는 예열에서 effort 생략으로 해결 확인.
분석 완료 후 뜨는 6,880·8,520급 쓰기 1건은 2단계(문서 생성용, 분석 결과 포함 프리픽스) 예열로 정상.

## 4인 개편 잔재 버그 — completedCount === 6 (치명, 수정 완료)

AgentsPage에 `allCompleted = completedCount === 6`이 하드코딩으로 남아
4인 체제에서 **완료 판정이 영원히 false** → 사건 자동 생성·개요 생성·문서 유형 선택이 전부 잠겼다.
진행률 표기 `/6`도 동일 잔재. 전부 `AGENTS.length` 기준으로 교체하고 운영에서 실측 검증
(4/4 완료 → 사건 자동 생성 → 문서 유형 선택 개방 확인 후 테스트 사건 삭제).
교훈: **집단 크기를 바꾸면 판정식·표기를 상수(AGENTS.length)로 묶었는지 전수 검색할 것** (`=== 6`, `/6` 등).

## 테스트 데이터 정리 결과

캐시검증테스트 1·2·3은 완료 판정 버그 때문에 애초에 사건이 생성된 적이 없음(Firestore 서비스 계정 조회로 0건 확인).
완료판정테스트(버그 수정 검증용)만 실제 생성됐고 앱 UI로 삭제 완료. 잔여 0건.

## 2026-07-30 티어 1 구현 노트

- **경비/매입 입력이 없던 이유**는 계좌 자동연동(CODEF)을 기다리다 수동 입력을 안 만든 것. CODEF 계약 종료로 수동 입력을 먼저 완성하고, 자동연동은 별도 조사 완료(docs/bank-card-auto-sync-research.md의 2026-07-30 갱신 섹션 — 단기 추천: 팝빌 계좌조회 월5천/계좌 + 홈택스 사업용카드 스크래핑, 중기: 금결원 오픈뱅킹 조회전용).
- 삭제 UX는 window.confirm 대신 **2단계(무장→3초 내 재클릭)** — 브라우저 자동화 테스트를 막지 않고 UX도 낫다.
- 문자 발송의 수신번호는 Case.clientPhone에 저장(발송 성공 시 자동 갱신). 의뢰인 CRM(티어 2)이 생기면 그쪽으로 이관.
- /api/notify/client는 requirePaidPlan을 태움 — 문자는 실비가 나가므로 무료 플랜 차단. 관리자/유료 판정은 plan.ts 공용 로직.

## 2026-07-30 티어 2 구현 노트

- **의뢰인 CRM은 컬렉션 신설 대신 cases 집계**로 구현. 이유: 마이그레이션·이중 저장 없이 가치(복수 사건 묶임·연락처)의 대부분을 얻는다. 주소·메모·태그가 필요해지면 clients 컬렉션으로 승격하고 clientPhone도 그쪽으로 이관.
- 통합 캘린더·문서고는 기존 컬렉션(deadlines/documents)의 ownerId 단독 쿼리라 복합 인덱스 불필요.
- **판례 API 조사 결론(2026-07-30)**: 엘박스(500만)·빅케이스(506만)·케이스노트(300만) 전부 데이터를 해자로 삼아 외부 판매 안 함. 격차(우리 231만 vs 500만)는 2021년 이후 하급심. 보강 경로는 ① AI허브 판결문 원문 25만 건(무료·영리 활용 가능·출처 명시) ② 판결서 인터넷열람 건당 1,000원 온디맨드 ③ 2023-01 이후 미확정 민사 공개 확대분. 슈퍼로이어 458만 건 = 자사 빅케이스 DB.
- SW 갱신 전파는 "새 배포 → 브라우저가 다음 내비게이션에서 sw.js 확인 → 즉시 활성화 → 그 다음 로드부터 새 번들". 즉 최대 2회 접속이면 반영되고, 구버전이 새 라우트로 접속하면 대시보드로 리다이렉트될 수 있음(라우터가 모르는 경로 → * → /).

## 2026-07-30 티어 3 구현 노트

- **성공보수 구간별 산정은 초과 누진 방식**(세율 구간처럼 경계까지 해당 비율, 마지막 경계 초과분은 마지막 비율). 실무 약정이 제각각이라 산정액을 수동 수정 가능하게 두고 "자동 계산으로 되돌리기"를 붙였다.
- **의뢰인 포털은 서명 페이지와 같은 보안 모델** — cases.portalToken(32자 hex)으로 서버(서비스 계정)가 조회, 클라이언트에 최소 필드만. 타임라인은 label만 노출(내부 메모 detail 제외). portalEnabled=false로 즉시 차단 가능. 케어 메시지는 서브컬렉션이라 v1에서 제외.
- **사건 삭제는 fee 문서를 남긴다**(의도된 보존). 테스트 사건을 지울 때는 fee도 함께 정리할 것 — 이번에 고아 fee 1건을 서비스 계정으로 삭제했다.
- 백업 ZIP에는 Storage 원본(녹음·첨부)과 installments 서브컬렉션이 빠져 있다(README에 명시). 필요해지면 2차 확장.

## 2026-07-30 알림톡 도입 안 함 (대표 결정)

카카오 비즈니스 채널을 개설하지 않기로 함. 의뢰인 발송 채널은 Solapi 문자(SMS/LMS)로 일원화.
향후 세션에서 알림톡을 다시 제안하지 말 것 — 재검토는 대표가 먼저 꺼낼 때만.

## 2026-07-30 hwpx 내보내기를 실무 서면 서식으로 교체

대표가 제공한 실제 서면(고소장.hwpx, 항소이유서.hwpx — docs/)에서 스타일 실측 후 hwpxExport.ts 교체.
- **실무 표준 실측값**: 본문 휴먼명조 12pt · 양쪽정렬 · 줄간격 250% / 제목 20pt bold 가운데 /
  소제목 12~14pt bold / 날짜·서명 오른쪽 정렬 / A4 여백 좌우 8504·상 5668·하 4252 (고소장과 동일, 기존 값 유지)
- 날짜("2026. 7. 30.")·서명("(인)"/"(서명)" 끝) 줄은 자동 오른쪽 정렬. **번호 목록 검사보다 먼저** 해야 함
  (날짜가 "2026." 번호 목록으로 오인되는 함정 — 테스트로 고정).
- 검증: hwpxExport.test.ts 7건 (폰트·크기·줄간격·정렬·여백). 견본 = 바탕화면 로케디_서식견본_준비서면.hwpx
- 참고: docs/의 실제 서면 파일들은 의뢰인 실명·사건번호가 든 자료이므로 **깃에 커밋 금지** (.gitignore 확인 필요)

## 2026-07-31 Starter 무료 개방 + 1인 사무실 포지셔닝 + 카카오 1:1 문의

**Starter 무료화의 진짜 작업은 서버 검증이었다.**
가격 표기만 바꾸면 무료 사용자가 AI를 못 쓴다 — `requirePaidPlan`이 free를 402로 막고 있었기 때문.
그래서 판정을 두 갈래로 나눴다(functions/api/_shared/plan.ts):
- `requireUsageQuota` — AI 분석(claude)·음성 변환(transcribe)용. 무료도 허용하되
  **이번 달 documents 수**가 3건 이상이면 402(QUOTA_EXCEEDED). 조회 실패 시 통과(서비스 정지 방지).
- `requirePaidPlan` — 문자 발송(notify/client)처럼 **실비가 나가는 기능은 유료 유지**.
서버가 세는 기준은 문서 생성 수 하나뿐이다. 분석만 하고 문서를 안 만드는 경우는 통과시킨다
(체험을 막지 않으면서 반복 남용은 막는 실용선). 한도 상수는 `PLAN_LIMITS.free`와 수동 동기화 필요.

**결제 상품에서 starter 제거** — confirm.ts의 PLAN_MONTHLY_PRICE에서 빼면 parseOrderId가 거부해
결제 자체가 차단된다(team과 같은 방식). 결제자 0명이라 안전.

**포지셔닝**: "법률사무소 업무 자동화" → "1인 변호사 사무실 운영 SaaS".
히어로 아이브로우·서브카피·SEO 메타 6개·크롤러 폴백 전부 동기화(클로킹 방지).

**카카오 1:1 문의**: 채널 URL을 src/config/contact.ts로 공용화(BugReportButton도 이걸 사용).
랜딩·SEO 페이지에 플로팅 버튼(KakaoChatButton). 카카오 노랑(#FEE500)은 이 버튼에만 쓴다 —
페이지 유일 색이라 오히려 눈에 띄고, 기능 인지가 즉각적이다.

## 2026-07-31 상담 플로우를 녹음/첨부 2단계로 분리

기존: 한 화면에 녹음 + 파일첨부 + 메모가 모두 있었다(step "record" 하나).
변경: `info → record(상담 녹음) → attach(자료 첨부) → agents` 4단계.
이유는 실제 순서다 — 상담 중에는 녹음만 하고, 상담이 끝난 뒤 서류를 챙긴다.

- 녹음 단계: 녹음 UI + **녹음 결과 목록**("녹음 완료 N건", 개별 삭제 가능).
  녹음 중에는 다음 버튼 비활성(정지 후 이동). 녹음이 없어도 넘어갈 수 있다
  (녹음 파일을 따로 갖고 있거나 메모만 쓰는 경우) — 버튼 문구가 "녹음 없이 자료 첨부"로 바뀐다.
- 첨부 단계: 앞 단계 녹음 건수를 상단에 요약 표시. 파일·폴더·카메라·메모.
  "이전"은 info가 아니라 record로 간다.
- `recordedNames` state로 직접 녹음한 파일을 구분한다(업로드 파일과 목록에서 섞이지 않게).
- 기존 사건에서 "추가 상담"으로 진입하면 여전히 record부터 시작(info 건너뜀).

## 2026-07-31 폰 녹음 중 전화 수신 대비 (조각 저장 · 중단 감지 · 이어하기)

문제: MediaRecorder 데이터를 메모리에만 들고 있어서, 폰에 전화가 와 브라우저가 정리되면
30분 상담이 통째로 사라졌다. 게다가 마이크를 빼앗겨도 화면은 "녹음 중"이라 멈춘 줄도 몰랐다.

대응 3종 (전화 수신 자체는 웹앱이 막을 수 없으므로 "잃지 않게" 만드는 쪽):
1. **조각 저장** — `services/recordingStore.ts`(IndexedDB). MediaRecorder.start(5000)로 5초마다
   조각을 받아 즉시 append. 1초는 쓰기가 너무 잦고 10초는 사고 시 손실이 크다.
   저장 실패는 throw하지 않는다 — 녹음 자체는 계속돼야 한다.
2. **중단 감지** — `stream.getAudioTracks().onended` + `mediaRecorder.onerror` → `interrupted` 상태.
   화면에 "녹음이 중단되었습니다" 배너. 이 이벤트가 없으면 사용자는 끝까지 모른다.
3. **이어하기** — 진입 시 저장된 조각(3초 초과)이 있으면 복구 배너. 불러오면 하나의 파일로 합쳐
   첨부 목록에 넣는다. 정상 정지로 파일을 확보하면 조각을 즉시 비운다(다음 진입 시 오배너 방지).

추가로 녹음 전 "방해금지 모드를 켜세요" 안내(닫기 가능). 전화를 아예 안 받는 게 가장 확실하다.
mimeType은 세션 메타에 저장해 사파리(audio/mp4) 확장자도 맞춘다. 테스트 6건(fake-indexeddb).

## 2026-07-31 히어로 배경 영상 (AI 생성 → 가공 → 삽입)

Higgsfield(kling3_0_turbo)로 "이슬 맺힌 새벽 페어웨이 매크로" 5초 생성(7.5크레딧, 16:9 720p).
원본은 어둡고 5초라 그대로는 못 쓴다. ffmpeg로 세 가지를 손봤다:
1. **밝기·톤 보정** — `eq=brightness=0.14:saturation=0.82:gamma=1.18` + colorbalance로 크림 배경에 녹임
2. **왕복 루프** — `split→reverse→concat`으로 정방향+역방향 10초. Veo류는 seamless loop를 못 만들어서
   이음새가 보이는데, 왕복이면 시작=끝이라 완벽히 이어진다. **다음에도 이 방법을 쓸 것.**
3. **무음·압축** — `-an -crf 31` → 3.5MB에서 **282KB**로. 배경 영상은 오버레이에 눌리므로 화질을 낮춰도 된다.

삽입(LandingPage 히어로):
- 좌→우 크림 그라디언트 오버레이(0.94→0.5)로 글자 영역만 두껍게 덮어 가독성 확보
- `prefers-reduced-motion` + **640px 이하에서는 영상을 숨기고 포스터 이미지(.hero-still)만** — 데이터·배터리
- 콘텐츠에 `relative z-10` 부여(영상이 absolute inset-0이라 안 하면 클릭이 막힌다)
파일: `public/media/hero-fairway.mp4`(282KB) · `hero-fairway.jpg`(47KB, poster 겸 모바일 대체)

## 2026-07-31 폰 화면 깨짐 4건 수정 (히어로 영상 도입 후 발견)

데스크톱만 보고 배포했다가 폰에서 네 가지가 한꺼번에 문제였다. **다음부터 랜딩 손대면 390px 폭으로 반드시 확인할 것.**
1. **스코어카드 좌우 잘림** — `rotate(-0.5deg)`가 좁은 화면에서 모서리를 밀어냈고, 내부 패딩(px-6)·폰트가 커서 12열 그리드가 넘쳤다.
   → 회전 제거, 모바일 패딩 px-3.5, 폰트 12px, 업무명 truncate.
2. **배경 영상이 안 보임** — 오버레이가 `linear-gradient(100deg, 0.94 → 0.5)` 가로 방향이라
   세로 화면에서는 왼쪽 0.94~0.88 구간이 화면 전부를 덮었다. → 모바일은 **세로 그라디언트**(위 0.95 → 아래 0.45)로 분기.
3. **메뉴가 통째로 사라짐** — 네비 링크가 `hidden md:flex`인데 햄버거가 없어 섹션 이동 불가였다.
   → 로고 줄 아래에 **가로 스크롤 칩 줄**(md:hidden). 열고 닫는 동작이 없어 탭 한 번이면 이동한다.
4. **카카오 버튼이 콘텐츠를 가림** → 모바일은 아이콘만(라벨 hidden sm:inline), 크기·여백 축소.

## 2026-07-31 공개 화면 톤 통일 + 운영 주체 표기 (카카오 반려 대응)

**톤 통일** — 폰 점검 중 화면마다 톤이 제각각인 게 드러났다.
- SeoPageLayout: 저울 아이콘 로고·둥근 사각 버튼·산세리프 → 랜딩과 같은 "이름 + 핀 깃발 점" 로고,
  pill 버튼, 명조 표제, 크림 지면. 푸터 연도도 2025 하드코딩 → `getFullYear()`.
- SEO 서브페이지 5종: 흰 둥근 카드(rounded-xl) → 각진 반투명 카드, 본문 글자색 통일.
- 로그인 화면: "변호사 AI 상담 어시스턴트"(옛 카피)·"Premium Legal AI Assistant"(영문 장식) 제거 →
  "1인 변호사 사무실 운영 SaaS", "구글 계정으로 로그인하면 Starter가 무료로 열립니다".

**카카오 비즈니스 채널 반려 대응** — 사유는 "사업자와 채널의 연관성 확인 불가"였다.
랜딩 어디에도 운영 주체가 사업자 정보와 함께 명시돼 있지 않았던 것이 원인.
→ 푸터에 **상호·대표·사업자등록번호·전화·주소**를 명시하고, 크롤러 폴백과
`Organization.parentOrganization`(구조화 데이터)에도 같은 내용을 넣었다.
전자상거래법상 유료 서비스의 표시 의무이기도 해서 어차피 필요했던 것.
값 출처: Firestore users(admin) — 사업자등록증 OCR 인증 시 저장된 값.

## 2026-07-31 미디어 파일은 이름을 바꿔서 교체할 것 (캐시 함정)

히어로 영상을 넓은 페어웨이로 바꿨는데 대표 화면에는 계속 옛 잔디 영상이 보였다.
서버는 새 파일을 정확히 주고 있었고(md5 일치), 원인은 **브라우저 HTTP 캐시**였다 —
`Cache-Control: public, max-age=14400`(4시간)이라 같은 URL이면 재요청조차 하지 않는다.
특히 모바일은 영상 대신 poster(jpg)를 쓰므로 **이미지 캐시까지 같이 물린다.**

→ 규칙: `public/` 아래 미디어를 교체할 때는 **덮어쓰지 말고 파일명에 버전을 붙일 것**
(`hero-fairway.mp4` → `hero-dawn-v2.mp4`). JS/CSS는 빌드가 해시를 붙여주지만
public 정적 파일은 그런 장치가 없다. 지난 `assets/index-*.js` 사고와 같은 계열의 함정.

## 2026-07-31 히어로 배경 고정(패럴랙스) + 모바일에서도 영상 재생

- **모바일 정지 이미지 대체 철회** — 640px 이하에서 영상을 숨기고 poster만 쓰게 했더니
  "동영상이 아니라 사진"으로 보였다. 최종 파일이 97KB라 데이터 부담이 사실상 없으므로
  모바일에서도 재생한다. 정지 대체는 `prefers-reduced-motion`일 때만 남긴다.
- **배경 고정** — `.hero-bg { position: fixed; inset: 0; z-index: 0 }`.
  스크롤하면 배경은 제자리, 글자·스코어카드만 위로 지나간다.
  fixed라 히어로를 벗어나도 화면에 남으므로 **뒤따르는 콘텐츠에 `relative z-10` + 자체 배경색**을
  줘야 덮인다(main·footer·consult 섹션에 부여). 안 하면 전 페이지에 영상이 비친다.
