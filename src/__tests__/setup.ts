// Test setup file
// Note: @testing-library/jest-dom is imported in component test files
// that use jsdom environment (via @vitest-environment jsdom comment).
// Pure function tests use the default node environment.
